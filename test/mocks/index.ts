import * as config from './config'
import * as state from './state'
import { Storage } from './storage'
import { mockApiConfig } from './api-config'

export default {
  Storage,
  config,
  state,
  mockApiConfig
}
